# HANDOFF — control_admon_poncitlan

Read this file first in any new session working on this project. It is a thin pointer to the canonical documentation, not a copy of it — the files it points to are the source of truth; this file is a map.

## What this project is

A club/organization expense-tracking app ("Control de Gastos Poncitlán"), being migrated to Vite + TypeScript (see below), live on Vercel, backed by a Supabase Postgres project named "arca" (ref `qjswicjxwsbwnxrrowsi`) that is **shared** with an unrelated system (a different, separate "arca" national-chapters project, still in approval — never touch its tables, listed by name in the Facts section below).

## Active change: `evolucion-plataforma-arca`

A full SDD (Spec-Driven Development) cycle, planned and almost fully executed via the `gentle-ai` ecosystem (`/gentle-sdd-*` commands / native `sdd-*` subagents). Turns the single-file app into a robust, tested, role-based platform: closes a critical open security hole (done), migrates to Vite+TypeScript (done), adds a superadmin/admin role model with TOTP MFA (done), adds a public no-login debt dashboard and a bank-account info panel (done), and adds member lifecycle management — retire/delete with history protection (Phase 4, code + live SQL done; only a manual live-session test remains).

**First command to run in a new session**: `gentle-ai sdd-status evolucion-plataforma-arca --cwd <repo> --json --instructions` — this is the authoritative, machine-readable status (dependency states, next recommended phase, blockers). As of this handoff it reports **36/38 tasks complete, `nextRecommended: apply`** (stays `apply`, not `verify`, until the two remaining manual-test tasks — 2.6 and 4.5 — are done; both need a human in a real logged-in session, not another `sdd-apply` batch). Trust its JSON over any prose summary, including this file, if they ever disagree.

## Archived change: `portal-miembros` (2026-09-14, DONE)

A second, separate SDD change — not part of `evolucion-plataforma-arca`, run to completion in one session and archived. Added two things: (1) an admin-only panel showing a member's full payment history (paid + pending `cargos`, all `registro_pagos`, works for retired members too — previously only pending charges were visible, as a side-effect of the payment form); (2) a public, no-login, per-member private view (`/mi-cuenta/?token=...`) where a member holding a unique link sees their own debts/contributions, backed by a new `api/member-view.ts` cloning the `api/debt-view.ts` pattern from `evolucion-plataforma-arca` Phase 3.

**This explicitly reverses `evolucion-plataforma-arca`'s own round-1 research decision** ("2-role model, no member login") — recorded as a deliberate, project-owner-confirmed override, not an oversight. The chosen mechanism is a **bearer token, not a login**: a 256-bit CSPRNG token, base64url-encoded, hashed with SHA-256 (no salt/KDF — justified in design.md D10, high entropy makes a slow KDF pointless and would force a full table scan) and stored **hash-only** on `miembros.token_hash` (+ `token_generado_en` for the UI's "Generado el ..." label). No email, no password, no new Supabase Auth surface — this was the deciding factor, since `miembros` has zero contact info and Phase 0 of `evolucion-plataforma-arca` closed public self-signup after a real incident.

- **Security properties, all independently verified live in production, not just in tests**: missing, malformed, unknown, and revoked tokens all return the exact byte-identical `404 {"error":"not_found"}` (no enumeration oracle); every response — success or failure — carries `Cache-Control: private, no-store`; `mi-cuenta/index.html` has `<meta name="referrer" content="no-referrer">` and a real-Chrome-tab check confirmed `document.referrer` is empty after following the link to `/vista/` (the token never leaks via the `Referer` header); `crypto.timingSafeEqual` is length-guarded; a 30-sample timing comparison between an unknown and a revoked token (both real live requests against production) showed medians within normal network noise of each other (~0.47s vs ~0.53s), confirming they're not distinguishable by response time, unlike the intentionally-faster missing-token short-circuit (~0.33s, accepted asymmetry).
- **Excluded from the member's own view, by spec, not just by design choice**: `motivo` (why they were charged) and `observaciones` — inherited transitively from `api/debt-view.ts`'s own confidentiality-motivated exclusion list (these can be medical/personal reasons concerning a third party). Revisiting this needs a spec amendment, not just a product sign-off.
- **Delivered as 4 chained PRs** (`auto-chain`/`stacked-to-main`, this project's cached delivery strategy): PR1 admin history (no DB), PR2 live DB schema (additive, sign-off gated, applied live 2026-09-14), PR3 token issuance UI, PR4 the public endpoint + portal page. Each PR passed an independent fresh-context adversarial verification before merge.
- Archived at `openspec/changes/archive/2026-09-14-portal-miembros/`; its three new capabilities (`member-payment-history`, `member-access-token`, `member-private-view`) now live in `openspec/specs/` alongside `evolucion-plataforma-arca`'s seven (still open, not yet archived).
- **Known operational gap, not a code gap**: no in-app way to deliver a member's link (no email/phone on file) — the admin generates it, copies it, and sends it out-of-band (WhatsApp, printed, verbal). Losing a link means the admin regenerates (the old one is immediately dead) — there is no recovery path, by design (hash-only storage).

## Archived change: `caja-y-transferencia` (2026-09-15, DONE)

A third, separate SDD change — not part of `evolucion-plataforma-arca` and distinct from the archived `portal-miembros`, though it builds on both (it extends the Phase 3 public debt-view surface and mirrors the `portal-miembros` CLABE/token affordances). It shipped three things: (1) a **derived cash-on-hand ledger ("Arca")** computed on read from existing and new ledger rows; (2) a **CLABE copy button** on the public no-login view (`vista/`), backed by a dependency-free `src/lib/clipboard.ts` so the public bundle gains no Supabase import; (3) an admin **caja/Arca module** (opening amount + breakdown + the single disbursement capture surface described below). Its delta specs cover two capabilities: `caja` and `public-clabe-copy`.

**Core domain rule to preserve**: `Arca (disponible) = Apertura + Σ(registro_pagos.monto_pagado) − Σ(registro_apoyos.monto_total) − Σ(registro_egresos.monto)`, computed by the pure `computeCaja` — there is **no stored running balance** anywhere. An **apoyo is a LOAN**: cash leaves, a receivable is created, and members repay it through `registro_pagos`, so a fully repaid apoyo nets to zero (and an unrepaid one leaves the Arca negative). An **egreso is an EXPENSE**: cash leaves and never returns. The decisive test is *"does the money come back?"* — which is exactly why the two outflow cards (`Apoyos entregados (recuperables)` and `Egresos (no recuperables)`, grouped under "Salidas del arca") are separate and must **never be merged**. **"Por cobrar" is contextual and is never summed into the balance.**

**Naming**: "Arca" / "Arca (disponible)" is a PRESENTATIONAL user-facing label only. Internal identifiers deliberately keep their names — `computeCaja`, `cajaCents`, `src/features/caja/`, `configuracion_caja`, `#caja-content`, `fetchCaja`, `initCaja`. The UI-says-Arca / code-says-caja asymmetry is INTENTIONAL; do not "fix" it by renaming identifiers or DOM ids.

**Single capture point**: leaving the arca has exactly ONE entry surface — "Solicitud de Apoyos" — with three modalities: group (`TODOS`/`FULLPARCH`), individual (`INDIVIDUAL`), and "Sin cargos — absorbido por el Arca (no recuperable)". The last writes one `registro_egresos` row and NO apoyo and NO cargos, with an OPTIONAL beneficiary. The **Arca page is a query/config surface only** (opening amount + breakdown); its duplicate "Registrar Egreso" form was removed (commit `f94757a`, now merged to `main` — see the closure section).

**Internal members**: `miembros.status` gained a third value `'interno'` (widened by a live migration). An internal member is excluded from every debtor surface (admin ranking + "Miembros con Deuda" KPI, public ranking, "Por cobrar") and from group divisions; it remains selectable for individual disbursements, and it is the usual BENEFICIARY of a non-recoverable expense.

**One debt aggregation**: the admin dashboard, the public view and "Por cobrar" must produce IDENTICAL per-member figures. They previously disagreed by cents because the dashboard had its own float reduce; the dashboard now reuses the pure `aggregateDebtByMember` over `estado='pendiente'` cents. **Two screens showing two numbers for the same debt is unacceptable for an auditable ledger.**

**Migrations applied live** (same operator-run + read-back pattern as Phases 0/2/3/4, each headed with its own pre-DDL guard, each with a `_down` reversal): `supabase/sql/phase5_egresos.sql`, `phase6_miembros_status_interno.sql`, `phase7_egresos_beneficiario.sql`, `phase8_reclasificar_gasto_sin_cargar.sql`. Note: these `phaseN_` filenames continue the numbering from `evolucion-plataforma-arca`'s Phase 4, but they belong to THIS change — they are not phases of `evolucion-plataforma-arca`. Phase 8 moved ONE misrecorded disbursement from the loan ledger to the expense ledger (guarded, atomic `begin;…commit;`, reversible); the Arca balance was unchanged because both are deductions — only the classification moved, and a phantom cargo disappeared.

**Status**: SHIPPED AND CLOSED — merged, deployed, LIVE, verified and **ARCHIVED 2026-09-15** (commit `4c4a8f3`). Its artifacts under `openspec/changes/caja-y-transferencia/` were reconciled to a single settled contract in PR #3 (`7b40509`), then verified and archived.

**Archive location**: `openspec/changes/archive/2026-09-15-caja-y-transferencia/` (8 files, incl. `archive-report.md`). Its two capabilities are now sources of truth at `openspec/specs/caja/spec.md` and `openspec/specs/public-clabe-copy/spec.md`, folded **byte-for-byte** from the archived deltas (SHA-256 verified on both sides). `gentle-ai sdd-status` reports `archive: all_done` / `nextRecommended: archived` / `blockedReasons: []`. Nothing is pending for this change.

## Where the documentation lives (canonical, versioned in this repo)

- `openspec/config.yaml` — project-wide SDD rules/guardrails; `strict_tdd: true`, `test_command: "vitest run"` (flipped in Phase 1, task 1.13)
- `openspec/changes/evolucion-plataforma-arca/`
  - `exploration.md`, `research.md` (8 official Supabase/Vercel sources) — investigation phase
  - `proposal.md` — accepted scope, phases, rollback plan
  - `design.md` — concrete architecture: exact SQL, module boundaries, sequence diagrams
  - `specs/*/spec.md` (7 files) — testable requirements per capability, Given/When/Then
  - `tasks.md` — **the live roadmap/bitácora**. Checkbox state (`[x]`/`[ ]`) IS the authoritative applied/pending log. Read this file to know exactly what's done. Also carries the Phase 0 sign-off record and the Phase 2 SQL-applied-live record, plus one unnumbered "Follow-up" note under Phase 2.
  - `phase0-account-audit.md` — record of the `auth.users` audit performed during Phase 0
- `supabase/sql/phase0_down.sql` — rollback for Phase 0 (re-enable signup, re-grant anon, recreate prior policies)
- `supabase/sql/phase2_{roles,seed_superadmin,rls,aal2,guard}.sql` — Phase 2's SQL, each headed `-- STATUS: APPLIED live 2026-09-13` with its own read-back confirmation
- `supabase/sql/phase2_down.sql` — rollback for Phase 2 (drops the guard trigger first, then the new policies, then recreates Phase 0's exact 8 policies verbatim, then drops `app_admins`/the two helper functions)
- `supabase/sql/phase2_manual_tests.sql` — already drafted, ready-to-run SQL for task 2.6 (aal1 refused / aal2 succeeds / anon denied / last-superadmin-guard cases), explicitly headed as not executed by any batch — a human runs this manually through a real session. Read/reuse this before writing new test SQL for 2.6.
- `supabase/sql/phase3_bank_config.sql` — `configuracion_bancaria` DDL + RLS, headed `-- STATUS: APPLIED live 2026-09-14` with its own read-back confirmation
- `supabase/sql/phase3_bank_config_down.sql` — rollback for Phase 3 (drops `configuracion_bancaria`; no prior policies/grants to restore, it's a brand-new table)
- `supabase/sql/phase4_activo_column.sql` — adds `miembros.activo boolean not null default true`, headed `-- STATUS: APPLIED live 2026-09-14` with its own read-back confirmation (10/10 existing rows backfilled to `true`)
- `supabase/sql/phase4_activo_column_down.sql` — rollback for 4.1 (drops the `activo` column)
- `supabase/sql/phase4_fk_restrict.sql` — flips `cargos_miembro_id_fkey`/`registro_pagos_miembro_id_fkey` from `CASCADE` to `RESTRICT`, headed `-- STATUS: APPLIED live 2026-09-14` with its own read-back confirmation; leaves `cargos_apoyo_id_fkey` (→ `registro_apoyos`) untouched at `CASCADE`
- `supabase/sql/phase4_fk_restrict_down.sql` — rollback for 4.2 (recreates both constraints verbatim with `CASCADE`)

## Current status — Phases 0, 1, 2, 3, and 4 are DONE and live. 36/38 tasks; only two human-driven manual-test tasks (2.6, 4.5) remain.

**Phase 0 (security fix) — DONE, live-verified, sign-off obtained.** Public self-signup closed (`disable_signup: true`), `anon` has zero grants on the 4 original tables, the 8 pre-Phase-2 `authenticated` policies were left untouched at the time (later replaced in Phase 2). `auth.users` audited: 9 accounts, none unrecognized. `fors@gmail.com` (project owner) is the sole superadmin as of Phase 2. `alexis@gmail.com` (former capturista, 105 linked financial records) was kept, not deleted, and lost elevated access naturally in Phase 2 by not being added to `app_admins`. Live-DB sign-off for this phase is recorded in Engram (project `control_admon_poncitlan`) and at the top of `tasks.md`'s Phase 0 section.

**Phase 1 (Vite + TypeScript migration) — DONE, tasks 1.1–1.13.** Vite + TypeScript + Vitest scaffold, `src/lib/money.ts`/`escape.ts` (built RED-GREEN, fixes the pre-existing float-rounding and epsilon bugs), and the full extraction of the original single-file monolith into `src/app.ts` + `src/main.ts` + 5 feature modules (`auth`, `dashboard`, `miembros`, `apoyos`, `pagos`). `openspec/config.yaml` flipped to `strict_tdd: true` here (task 1.13) — governs every `sdd-apply`/`sdd-verify` run from this point forward. Behavior preserved except the two named money/escaping fixes and an intentional new pagos overpayment block/warning (task 1.11).

**Phase 2 (Roles + MFA) — DONE, including the UI, live, pushed.** All 8 tasks complete except 2.6 (see Known gaps below):
- `app_admins` table + `is_admin()`/`is_superadmin()` helpers (`security definer`, `search_path=''`) — applied live.
- RLS overhaul: the original 8 blanket policies (4 `ALL` + 4 `SELECT`, on `miembros`/`cargos`/`registro_apoyos`/`registro_pagos`) were **all** dropped — not just the 4 `ALL` ones the task text literally named; leaving the `SELECT` ones would have left non-admin reads open (Postgres OR-combines permissive policies). Replaced with `is_admin()`/`is_superadmin()`-scoped policies on those 4 tables plus `app_admins` itself (5 tables total — `configuracion_bancaria` doesn't exist until Phase 3).
- AAL2 restrictive layer on `app_admins` writes only (insert/update/delete — never a bare FOR-less restrictive policy, which would default to `FOR ALL` and block every session's own-role read).
- Last-superadmin guard trigger (statement-level, not RLS `USING`).
- `fors@gmail.com` seeded as the sole superadmin.
- **Effect**: `fors@gmail.com` is now the only account with any access to `miembros`/`cargos`/`registro_apoyos`/`registro_pagos`. `alexis@gmail.com` and every other account lost access the moment the RLS script ran. Re-granting needs an `insert into app_admins` row for that user — there's a UI for this now (see next point), but it requires the raw `auth.users` UUID (see Known gaps).
- The admin panel and forced-MFA-enrollment screen (`src/features/admin/*`, `src/features/auth/mfa.ts`) are written, unit-tested, **and mounted into `main.ts`/`index.html`** — reachable via a nav link (hidden for non-admins) and a login-time gate that blocks a superadmin without verified TOTP from reaching the dashboard until they enroll.
- Live execution used the Supabase Management API `database/query` endpoint (same as Phase 0), with a read-back verification after every one of the 5 SQL files before the next ran. Full trail in Engram topic `sdd/evolucion-plataforma-arca/apply-progress`.

**Day-to-day impact for `fors@gmail.com`**: none — `is_admin()` (used by miembros/cargos/apoyos/pagos policies) has no AAL requirement, so normal dues/payment tracking works exactly as before at aal1 login. Only writes to `app_admins` itself (managing other admins) require AAL2.

**Phase 3 (Public View + Bank Config) — DONE, live, verified.** All 6 tasks (3.1–3.6) complete. `configuracion_bancaria` singleton table + a single `is_admin()`-scoped `FOR ALL` RLS policy (deviation from design.md's literal 2-row RLS table forced by Postgres syntax — `CREATE POLICY`'s `FOR` clause can't take a comma list — semantics are identical) was drafted, signed off, applied live 2026-09-14, and read-back verified (`pg_policies` = 1 row, `anon` grants = 0, seed row present). The admin-only bank-config UI, the `service_role`-backed `api/debt-view.ts`, the `postbuild` service_role-leakage guard, and the public `vista/index.html` + `src/public-view.ts` entry are implemented and unit-tested (152/152 passing). `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are set in Vercel (Secret type, Production+Preview). Pushed to `origin/main` (commits `a116c88`, `ad264a3`, `58b1595`, `62338c5`, `7532aad`).

**Live-deploy bug found and fixed during 3.6's E2E check**: `api/debt-view.ts` 500'd (`ERR_MODULE_NOT_FOUND`) on first deploy with the new env vars — Vercel transpiles `api/*.ts` per-file with the project's own `tsc` and runs the output as plain Node ESM, which (unlike Vite's `moduleResolution: "bundler"`) requires explicit `.js` extensions on relative imports. Fixed in `api/debt-view.ts` -> `src/lib/debt-view.ts` -> `src/lib/money.ts` (commit `7532aad`); also hardened the handler with a top-level `try/catch` so any future unhandled error returns the documented `502`, not a raw Vercel `500` (commit `62338c5`). **If a future phase adds another `api/*.ts` file that imports from `src/lib/`, it needs `.js`-suffixed relative imports too** — this is a structural gotcha of the zero-config Vercel + Vite setup, not a one-off; see this same fact repeated in the Facts section below.

E2E verified live (production — corrected: Vercel DOES create a preview deployment per branch; this project's rule is to merge to `main` and let Vercel deploy `main`, never to promote a mid-chain preview — see the Vercel deploy-safety note below): `/vista/` renders correctly with no session (screenshot-confirmed: total pendiente, bank info, ranking de deudores, no login/admin UI, no console errors), both `/vista/` and `/api/debt-view` resolve `200`. XSS-escaping was **not** re-verified by inserting a live payload into production `miembros` — `public-view.ts`'s use of `escapeHtml` on every user-controlled field was confirmed by code review, and `escapeHtml` itself is already exhaustively unit-tested (Phase 1).

**Phase 4 (Lifecycle) — DONE, code + live SQL, not committed yet at the time of this handoff edit; check `git log` for whether it landed.** All 5 tasks except 4.5 (see Known gaps below):
- `src/features/miembros/{index.ts,repo.ts,errors.ts}` — retire/reactivate (`activo` toggle, reversible) + delete-with-confirm (`window.confirm`) mapping Postgres `23503` → "Este miembro tiene historial financiero; usa Retirar." Full TDD.
- `src/lib/miembros.ts`'s `activeMiembros()` — pure filter, wired into `apoyos/index.ts`'s candidate list/checkboxes and `pagos/index.ts`'s member selector. `src/lib/debt-view.ts`'s `aggregateDebtByMember` excludes `activo=false` rows; `api/debt-view.ts`'s query extended to select `activo` (kept the `.js`-extension convention on its relative imports — verified not regressed).
- **Deliberate scope note, not a bug**: the internal admin dashboard's "top deudores" KPI (`dashboard/index.ts`) still lists retired members with outstanding debt. Task 4.4 only scoped the apoyo candidate list, pago selector, and the *public* debt-view — not this internal KPI. Decision made in-session: leave it as-is, since the spec's intent is that a retired member's financial history "stays intact and queryable," and an admin-only debt KPI arguably should still surface money actually owed by a retired member. Revisit only if the project owner wants retired members to disappear from internal reporting too, not just from new-activity selection surfaces.
- Live SQL (`phase4_activo_column.sql`, `phase4_fk_restrict.sql`) applied 2026-09-14 with explicit project-owner sign-off, same gate as Phases 0/2/3 — see their headers and `tasks.md`'s 4.1/4.2 entries for the exact read-back confirmations (column backfill, FK delete-rule flip, `cargos.apoyo_id` untouched).
- Verified by a fresh-context independent review agent (re-ran `vitest run` and `tsc --noEmit` itself rather than trusting the apply agent's self-report; grepped for any live-mutating SQL code path — none found): verdict **SAFE TO PROCEED**, no defects.

## Known gaps — not done, not blockers, pick up whenever

- **Task 4.5** (manual SQL test: delete-with-history refused via `23503`, delete-with-no-history succeeds, retire preserves history) is unblocked now that 4.1/4.2 are live, but was deliberately left for a human-driven live-session test rather than run via the Management API — the "succeeds" case means actually deleting a real member row, which is outside what schema/DDL sign-off authorizes. Same nature as 2.6 below; consider batching both into one live-session pass.
- **Task 2.6** (manual RLS test as real anon/aal1/aal2 sessions) is undone by design: the Management API used to apply Phase 2 runs as an elevated role that bypasses RLS entirely, so it structurally cannot exercise this test. The exact test SQL is already drafted in `supabase/sql/phase2_manual_tests.sql` — needs a human (or a future live-session test) logging into the actual app as `fors@gmail.com`, enrolling MFA, and running it for real.
- **No email→UUID lookup for adding admins**: `addAdmin` (in the now-wired admin panel) takes a raw `auth.users` UUID the operator must already have — self-signup is closed (Phase 0), so there's no browser-safe way to look up a user by email. Flagged since task 2.8, still open; needs a decision in a future phase (e.g. a small service-role lookup endpoint).
- **No per-session AAL2 re-challenge UI**: if a superadmin's client-side session is only aal1 (e.g. a fresh login before Supabase re-confirms AAL2), an attempted write to `app_admins` fails with a raw RLS error rather than a friendly re-auth prompt. The actual security boundary (the DB policy) holds regardless — this is a UX gap only. Not fixed; flagged in `tasks.md`'s Phase 2 follow-up note.
- Tailwind is still served via the old CDN `<script>` tag in `index.html`; `@tailwindcss/vite` is installed (Phase 1) but not wired to a real CSS entry point yet. Cosmetic/tooling debt, not a blocker.

## Vercel — env vars set, live site confirmed working

`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` are set (as **Config** type — Vercel rejects `VITE_`-prefixed vars set as **Secret** type, since Secret is write-only and can't be inlined into the client bundle at build time; this bit the first Phase-1 deploy and was fixed in-session). See Engram discovery `discovery/vercel-vite-env-vars-must-use-config-type-not-secret` — any future `VITE_*` var on this Vercel project must use Config, never Secret.

## Vercel deploy safety — NEVER promote a mid-chain preview to Production

This file previously asserted "this project pushes straight to `main`, there's no separate preview-deploy flow in use". **That is no longer accurate and it is dangerous.** Vercel DOES produce a preview deployment per branch.

**The incident it caused**: in one session a **mid-chain preview deployment was promoted/rebuilt as Production**, so production served an unreviewed, mid-chain feature-branch commit while `main` was untouched and both PRs were still unmerged. Because the deployed code predated the formula change that adds the apoyo-deduction term, the site publicly displayed a cash figure roughly **ten times** the real value. (Figures deliberately not recorded here — this repo is public.) It was fixed by merging the PRs so Vercel deploys `main`.

**Rule, stated explicitly**: in this project, never promote a mid-chain preview to production. Merge to `main` and let Vercel deploy `main`. Do NOT tell a future session to "click Redeploy" without naming precisely which button — the same menu contains options that rebuild a deployment AS production, which is exactly how the incident above happened.

## Native SDD attempt ledger (`gentle-ai sdd-attempt`)

Tripped `changed_line_budget_exceeded` on several batches (Phase 0, Phase 1's two PRs) because it counts the raw git diff — including generated files like `package-lock.json` — against a cap the orchestrator sets per work unit. Each time required a `sdd-attempt reset` with the project owner's explicit in-session authorization (the tool's own guard requires this — "maintainer_decision", never automatic). Phase 2's batches did NOT trip this (no lockfile changes, diffs stayed under budget). Before assuming a real scope problem, check `gentle-ai sdd-attempt status --cwd <repo> --change evolucion-plataforma-arca` for the authored-vs-raw accounting, and always ask the project owner before resetting — never self-authorize.

- **`--diagnosis`, `--reason`, `--cleanup-evidence`, and `--process-evidence` have a ~500-byte cap.** An over-long field makes the command **silently no-op** — the attempt stays `running` and looks settled. **Always run those commands UNFILTERED** so a no-op cannot be mistaken for success. This tripped four times in one session; treat a suspiciously clean result as suspect, not as done.
- **The ledger charges TRACKED DOCUMENTATION to an attempt's budget too.** Once a change's `openspec/` artifacts are committed, every phase pays for the artifact it rewrites (one verify attempt measured 157 changed lines against an 80-line cap). Budget accordingly, and prefer setting the cap from a MEASUREMENT rather than an estimate — and note the repo's own convention above: ask the project owner before a `sdd-attempt reset`, never self-authorize.
- **Moving between work units has TWO branches, and they are NOT the same.** (a) Previous objective **FAILED** → `acquire` refuses with *"SDD runtime objective changed without an explicit reset"* and prescribes `sdd-attempt reset --expected-revision <rev from status>`. That is a **maintainer decision**: ask the project owner, never self-authorize. It publishes a new ledger revision and preserves all attempt history. (b) Previous objective **COMPLETE** → a passing `settle` returns `state: complete`, and the next objective is taken with a plain `acquire` using a **different** work-unit label; no reset is needed ("`rescope` applies only to an objective that is not complete, and `reset` discards this scope instead of succeeding it"). Do NOT dodge branch (a) by re-submitting the old objective's wording to steal its remaining attempt — that misrepresents the ledger.
- **Untracked files block both `acquire` and `settle`.** Declare `--untracked-scope=exclude --expected-untracked-inventory=sha256:<hash>` when no work lives untracked, or `--untracked-scope=select --intended-untracked=<path>` (repeatable) when the phase's OUTPUT **is** the untracked set — e.g. an archive, which produces only untracked paths. Using `exclude` there would record the work as "no change at all"; the tool blocks that, correctly. The recurring friction here was an untracked `.codegraph/` index (its own `.gitignore` self-ignores its contents, leaving just that one file) — now covered by this repo's `.gitignore`.
- **A `settle` can carry a binding obligation.** After an unremediated failed verification, the passing settle MUST pass `--remediates-evidence-revision <failed-rev>` with evidence **distinct** from it.
- **Set `--max-changed-lines` from a MEASUREMENT — and distrust the measuring tool.** `Get-Content | Measure-Object -Line` **undercounts**, because blank lines contribute 0 lines (it read a 305-line spec as 200). Use `(Get-Content).Count`. Relatedly: git detects pure renames, so a `git mv` of an 800-line directory costs **0** changed lines — an archive is far cheaper than its file count suggests. The `caja-y-transferencia` closure measured ≈864.

## Git

Everything through `portal-miembros`'s archive (code, all live SQL, docs) is committed AND pushed to `origin/main` as of commit `838902a` (Phase 4 of `evolucion-plataforma-arca`: code `22a4112`, SQL-applied status `ee9f6fa`; `portal-miembros`: PR1 `abbaa8e`, PR2 draft `b748e6c`, PR2 live `2d6dccc`, PR3 `2b0bc2f`, PR4 `c791720`, manual-test closure `cc03f2c`, verify report `f0de048`, archive `838902a`) — if `git status --short` ever shows any of these files as modified in a fresh session, someone edited it locally after cloning; don't assume it's still in flight. The default `gh`/git identity on this machine is `consultores-orion`, which does NOT have write access to this repo — all pushes used a one-off classic PAT for `skyclick1939` (stored in Engram, topic `config/github-pat-skyclick1939-control-admon-poncitlan`; the classic `ghp_`-prefixed token, `#2898` at last check — a superseded fine-grained PAT is in the same topic history, do not reuse it), passed via an inline authenticated URL rather than `gh auth login`, specifically to avoid disrupting `consultores-orion`'s default auth for other projects on this machine. Use the same pattern for future pushes.

**`caja-y-transferencia` closure (2026-09-15)**: the verify report, the V.3 evidence, the archive move, the folded specs and the archive report were committed and pushed to `main` as `4c4a8f3`, followed by a docs commit recording this closure in `HANDOFF.md` and adding `.codegraph/` to `.gitignore`. Vercel redeploys `main` on a docs-only commit with no change to the app output. A local-path/username string that had leaked into a captured test-output block in the archived `verify-report.md` was redacted in the same closure — this repo is public, so keep machine paths out of captured output.

**Gotcha confirmed this session**: pushing via an inline authenticated URL (rather than `git push origin main`) updates the real GitHub remote correctly, but does **NOT** update the local `refs/remotes/origin/main` tracking ref — so `git status`/`git log` may falsely claim the local branch is "ahead of origin/main by N commits" even when everything is genuinely pushed. Don't trust that message after a session that used this push pattern; verify with `git ls-remote https://github.com/skyclick1939/control_admon_poncitlan.git refs/heads/main` (ground truth) or just run a read-only `git fetch origin` to refresh the stale ref before concluding anything is actually missing.

## Facts a new session must NOT re-derive (already investigated, would waste time re-checking)

- Money columns (`monto_*`) are already Postgres `numeric`, not `float8` — no schema migration was needed for the rounding fix, only `money.ts`.
- AAL2 (TOTP-verified sessions) **can** be enforced at the Postgres RLS layer via `restrictive` policies reading `(select auth.jwt()->>'aal') = 'aal2'` — confirmed against official Supabase docs, and now live (Phase 2). Real DB-level boundary, not just a client-side UI gate.
- The app is hash-routed (`#dashboard` etc.), not path-routed — no `vercel.json` was needed even for the public dashboard entry (Phase 3, `vista/index.html`, its own separate path served by Vite's multi-entry build, not the hash router).
- The Supabase Management API token is stored in **Engram only**, never in any repo file — search Engram for the `config/supabase-management-token-control-admon-poncitlan` topic. Account-wide-scope credential; treat it accordingly. The exact execution pattern used for both Phase 0 and Phase 2 live changes: a small one-off Node.js script (not committed) reads a `.sql` file and POSTs it to `https://api.supabase.com/v1/projects/{ref}/database/query` with the token as a Bearer header — avoids shell-escaping SQL containing quotes/`$$` into `curl -d` strings. Delete the helper script after use; never commit it.
- The other, unrelated project(s) sharing this Supabase instance own tables named `arca_audit_logs`, `arca_chapters`, `arca_debts`, `arca_system_config`, `arca_user_profiles`, `telegram_whitelist` (all confirmed live via a `pg_policies` query during Phase 2's pre-check — but that query only surfaces tables that have at least one RLS policy, so it is not a complete table inventory) and `n8n_chat_histories` (named in `openspec/config.yaml`'s protected-table guardrail but not confirmed live by that same query, likely because it has no RLS policy defined — do not treat its absence from the live-confirmed list as evidence it doesn't exist). Treat the union of both lists as off-limits; if a future live-DB task needs a full table inventory, query `information_schema.tables`, not `pg_policies`.
- Claude Code sessions on this account can hit a weekly API rate limit during a long, tool-call-heavy session (happened once during Phase 2's UI-wiring batch). If a delegated sub-agent is cut off mid-task by this, check `gentle-ai sdd-attempt status` before assuming work was lost — its own `settle` call may have already landed before the interruption; only a trailing step (like an Engram save) might be missing.
- Any `api/*.ts` file's relative imports into `src/lib/` MUST use explicit `.js` extensions (e.g. `from '../src/lib/money.js'`, not `'../src/lib/money'`). Vercel transpiles `api/` files per-file with the project's own `tsc` and runs the output as plain Node ESM — it does NOT bundle the module graph like Vite does, and Node's ESM loader doesn't resolve extensionless specifiers. `tsconfig.json`'s `moduleResolution: "bundler"` already supports `.js`-suffixed imports resolving to `.ts` source, so this needs no config change; it bit `api/debt-view.ts` in Phase 3 (`ERR_MODULE_NOT_FOUND` → raw 500) and will bite any future `api/*.ts` file that imports from `src/lib/` without the extension.
- A TypeScript union may mirror a live Postgres `CHECK` constraint. Widening the type without widening the constraint leaves the feature inert with `23514` (`miembros_status_check`). Check for a constraint whenever an enum-like union changes — `Miembro.status`'s `'interno'` widening needed both the TS edit AND `phase6_miembros_status_interno.sql`.
- A terminal `sdd_task_result_empty` is a **TRANSPORT failure** (the child returned nothing), not a gate failure: it latches the SDD dispatch **for that session only**, so the continuation is a fresh session — and it is explicitly NOT a Gentle AI defect worth reporting. Do not retry in the latched session, and do not assume the artifacts are fine just because the dispatcher reports `done` (it reports existence, not coherence).

## SDD session preflight (will be asked again in a new session — answer the same way for continuity)

The `gentle-ai` SDD workflow requires a one-time-per-session preflight (Pace / Artifacts / PR strategy). The most recent session answered: **Automatic** pace, **openspec** artifacts (repo-local; the native dispatcher confirms `artifactStore: openspec`), **auto-chain** PR strategy with **stacked-to-main** chaining. Re-select the same options unless something changed.

## Governance note

Receipt-driven development (native code review) is **off** for this repo (`gentle-ai review mode status` confirms). Delivery follows ordinary repository policy — no native review gate blocks anything here. This is the user's own choice; don't second-guess it.

## What's next: close out the two manual-test tasks, then archive

All coding and live-SQL work for this entire change is done (36/38 tasks). The only path to `sdd-archive` runs through a human live-session test pass — no further `sdd-apply` batch is needed:

- **Task 4.5** — log in to the real app, retire a member (confirm history stays visible), attempt to delete a member with `cargos`/`registro_pagos` history (expect a friendly refusal from the `23503` mapping, not a raw error), then delete a member with zero financial history (expect it to actually succeed — pick a genuinely disposable test row, not a real club member, or accept that this step is irreversible for whichever row is used).
- **Task 2.6** — log in as `fors@gmail.com`, enroll MFA, then run `supabase/sql/phase2_manual_tests.sql` for real (aal1 refused / aal2 succeeds / anon denied / last-superadmin-guard cases).
- Once both are done and checked off, `gentle-ai sdd-status` should flip `verify`'s dependency state to ready — run `sdd-verify` then `sdd-archive` to close the change.

## Closed 2026-09-15: `caja-y-transferencia` (verify → archive, DONE)

Verified and archived in commit `4c4a8f3`; nothing is pending. Recorded here so a future session does not re-open it:

- **V.3 (negative-balance rendering) was EXERCISED DELIBERATELY against production — not merely code-inspected.** Because the Arca is positive in normal operation, the negative path is unreachable without a temporary opening: `configuracion_caja.monto_apertura` was set to a synthetic value so the derived Arca was exactly −1,000.00, and a real browser confirmed — on all THREE surfaces (admin dashboard "Arca (disponible)" KPI, admin Arca module breakdown, and public `/vista/` with no session) — that the figure rendered **red and unblocked** (never hidden, never clamped to zero, no "insufficient balance" guard). That same live pass independently confirmed two domain rules: the two outflow cards stay SEPARATE under "Salidas del arca", and "Por Cobrar" is displayed as context and NOT summed into the balance.
- **The test was fully reversible and left ZERO trace**: only `monto_apertura` was ever written, so reverting that one column restored the row exactly — `updated_by`/`updated_at` were never touched. Confirmed by read-back. Caveat: the public endpoint's edge cache (`max-age=60` + `stale-while-revalidate=300`) can lag the revert by up to ~1 minute — documented staleness, not a defect.
- **The gatekeeping was independent, not self-reported**: the tallies were re-counted from the spec files (19 `### Requirement:`, 36 `#### Scenario:` — exact match to the report) and `npx vitest run` was re-run by the orchestrator (13 files / 190 passed). The report on disk before this was a **stale `fail`** predating the reconciliation (`7b40509`) — that stale report is why native status had shown `verify: blocked`.
- **One ledger `reset` was consumed** (owner-authorized; never self-authorized) — see the ledger section for the two distinct branches.
3. **Still OUTSTANDING, not blocking** (unchanged): the legacy sub-cent `cargos` (the operator's discontinued "cents placeholder" practice — it creates cents-level phantom debt on real members; cleanup recommended, not performed) and the deferred **YEAR FILTER**, a reporting model rather than a filter, pending three decisions: (i) flows vs balance (the Arca is cumulative, so a period figure means "at year-end"); (ii) the aperture has no effective date (it must be defined as "balance before the first recorded movement"); (iii) "Por cobrar" is not historically reconstructible because payments are recorded per MEMBER, not per CARGO — the FIFO allocation is never persisted.

**Closing lesson (it recurs)**: a GitHub PR can report `mergeable: unknown` immediately after a push, and a merge attempt at that instant fails with "Pull Request is not mergeable" even when there is **no conflict at all**. The reliable move is to wait for the state to settle or test the merge locally first, then retry — do not start resolving a conflict that does not exist.
