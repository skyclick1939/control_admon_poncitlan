# Specs

Source-of-truth specifications for `control_admon_poncitlan`, organized by domain
(`{domain}/spec.md`). Empty until the first change is archived by `sdd-archive`,
which merges its delta spec into the matching domain here.
